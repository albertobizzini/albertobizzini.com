@using KindleClippings
@inject IStringLocalizer<KindleClippingOfTheDay> L

<MudPaper Elevation="6" Class="pa-3">
	@if (_clipping is null)
	{
		<MudSkeleton />
		<MudSkeleton />
	}
	else
	{
		<MudText HtmlTag="em" Color="Color.Info">@_clipping.QuotedText</MudText>
	}
</MudPaper>
@if (_clipping is not null)
{
	<MudText Typo="Typo.caption" Align="Align.Right" Class="mt-3" Style="display:block">
		@L["ExtractedFromLabel"] <MudText Color="Color.Info" Inline>@_clipping.Book.Title</MudText>
		@if (_clipping.Book.Author is not null)
		{
			<MudText Inline="true"> (@_clipping.Book.Author)</MudText>
		}
		@if (_clipping.AddedOn is not null)
		{
			<MudText Typo="Typo.caption" Class="opacity-med" Style="display:block">(@L["AddedOnLabel"] @_clipping.AddedOn.Value.ToShortDateString() - @L["PositionLabel"]: @_clipping.StartLocation)</MudText>
		}
	</MudText>
}

@code {
	[Parameter]
	public Clipping Clipping { get; set; }
	private Clipping _clipping => Clipping;
}